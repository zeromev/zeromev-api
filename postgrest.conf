db-uri = "postgres://postgres:your_postgres_password@localhost:5432/zmapi" # set your password here
server-host = "127.0.0.1"
db-schemas = "public"
db-anon-role = "web_anon"


# All the "warn" level events plus all requests (every status code) are logged
log-level = "warn"

